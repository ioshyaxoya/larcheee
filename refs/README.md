# refs/ — референсы, не ассеты

Здесь лежат PNG от генератора и превью трассировки. **В игру отсюда не идёт
ничего:** движок читает только контуры из `data/`. Рядом с каждым PNG лежит
`.prompt.txt` — чем он сделан. Реестр с ценами — `docs/asset_ledger.md`.

Как это работает — `docs/asset_prompt_template.md` и
`.claude/skills/asset-gen/SKILL.md`.
